services:
  - type: web
    name: discord-bot
    env: python
    runtime: python
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    plan: free
